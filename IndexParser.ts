import { JSDOM } from "jsdom";
import axios from 'axios';


/*type Index {
    
}
const URL = 'https://www.marxists.org/';

export default async function indexParser(indexUrl:string) : Index{
  const response = await axios.get(URL + indexUrl, { responseType: 'text', responseEncoding:'binary' });
  const index = new JSDOM(response.data);
  return undefined;  
}*/